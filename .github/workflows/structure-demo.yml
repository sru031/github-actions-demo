name: Workflow Structure Demo

on:
  push:

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Step One
        run: echo "This is step one"

      - name: Step Two
        run: echo "This is step two"
