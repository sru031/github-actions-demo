name: Jobs and Execution Order Demo

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "Building application"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test ste
        run: exit 1

  deploy:
    needs: [build, test]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy step
        run: echo "Deploying after build and test"

    
